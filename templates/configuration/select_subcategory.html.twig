{% extends 'base.html.twig' %}

{% block title %}Configuration Portes/Fenêtres - Sélection de la sous-catégorie{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="row">
        <div class="col-md-8 mx-auto">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0">
                        <i class="bi bi-folder2-open me-2"></i>
                        Sélection de la sous-catégorie
                    </h4>
                </div>
                <div class="card-body">
                    <div class="mb-4">
                        <h5 class="text-muted">Projet : {{ projet.refClient }}</h5>
                        <p class="text-muted">{{ projet.description }}</p>
                    </div>

                    <div class="alert alert-success">
                        <div class="d-flex align-items-center">
                            <i class="bi bi-check-circle-fill me-2"></i>
                            <div>
                                <strong>Produit :</strong> {{ produit.nom }}<br>
                                <strong>Catégorie :</strong> {{ categorie.nom }}
                            </div>
                        </div>
                    </div>

                    <div class="alert alert-info">
                        <i class="bi bi-info-circle-fill me-2"></i>
                        Étape 3/3 : Choisissez maintenant la sous-catégorie pour affiner votre sélection.
                    </div>

                    {% if sousCategories|length > 0 %}
                        <form method="post">
                            <div class="row g-3">
                                {% for sousCategorie in sousCategories %}
                                    <div class="col-lg-4 col-md-6">
                                        <div class="card border subcategory-card" data-subcategory-id="{{ sousCategorie.id }}">
                                            <div class="position-relative subcategory-image-container">
                                                {% if sousCategorie.urlImage %}
                                                    <img src="{{ sousCategorie.urlImage }}" 
                                                         alt="{{ sousCategorie.nom }}" 
                                                         class="card-img-top subcategory-image">
                                                {% else %}
                                                    <div class="subcategory-placeholder d-flex align-items-center justify-content-center">
                                                        <i class="bi bi-folder-fill text-info" style="font-size: 3.5rem;"></i>
                                                    </div>
                                                {% endif %}
                                                <div class="overlay">
                                                    <div class="overlay-content">
                                                        <i class="bi bi-check-circle-fill"></i>
                                                    </div>
                                                </div>
                                            </div>
                                            <div class="card-body text-center p-3">
                                                <h6 class="card-title mb-2">{{ sousCategorie.nom }}</h6>
                                                <small class="text-muted d-block">
                                                    <i class="bi bi-folder me-1"></i>
                                                    {{ categorie.nom }}
                                                </small>
                                                <input type="radio" name="sous_categorie_id" value="{{ sousCategorie.id }}" class="d-none subcategory-radio">
                                            </div>
                                        </div>
                                    </div>
                                {% endfor %}
                            </div>

                            <div class="d-flex justify-content-between mt-4">
                                <a href="{{ path('app_configuration_pf_category', {'projet': projet.id, 'confpf': confpf.id}) }}" 
                                   class="btn btn-secondary">
                                    <i class="bi bi-arrow-left me-2"></i>
                                    Changer de catégorie
                                </a>
                                
                                <button type="submit" class="btn btn-success btn-lg" id="submit-btn" disabled>
                                    <i class="bi bi-check-circle me-2"></i>
                                    Finaliser la sélection
                                </button>
                            </div>
                        </form>
                    {% else %}
                        <div class="alert alert-warning text-center">
                            <i class="bi bi-exclamation-triangle me-2"></i>
                            Aucune sous-catégorie disponible pour cette catégorie.
                            <br>
                            <div class="mt-3">
                                <a href="{{ path('app_configuration_pf_category', {'projet': projet.id, 'confpf': confpf.id}) }}" 
                                   class="btn btn-secondary me-2">
                                    <i class="bi bi-arrow-left me-2"></i>
                                    Choisir une autre catégorie
                                </a>
                                <a href="{{ path('app_configuration_pf_details', {'projet': projet.id, 'confpf': confpf.id}) }}" 
                                   class="btn btn-primary">
                                    <i class="bi bi-arrow-right me-2"></i>
                                    Continuer sans sous-catégorie
                                </a>
                            </div>
                        </div>
                    {% endif %}
                </div>
            </div>

            <!-- Progress indicator -->
            <div class="card mt-3 border-0">
                <div class="card-body py-2">
                    <div class="progress" style="height: 8px;">
                        <div class="progress-bar bg-success" role="progressbar" style="width: 100%"></div>
                    </div>
                    <div class="d-flex justify-content-between mt-2">
                        <small class="text-success fw-bold">✓ Produit</small>
                        <small class="text-success fw-bold">✓ Catégorie</small>
                        <small class="text-primary fw-bold">→ Sous-catégorie</small>
                        <small class="text-muted">Configuration</small>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
.subcategory-card {
    transition: all 0.3s ease;
    cursor: pointer;
    overflow: hidden;
    border-radius: 12px;
    height: 100%;
}

.subcategory-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 12px 30px rgba(0,0,0,0.15);
    border-color: #198754 !important;
}

.subcategory-card.selected {
    border-color: #198754 !important;
    border-width: 3px;
    transform: translateY(-8px);
    box-shadow: 0 12px 30px rgba(25, 135, 84, 0.4);
}

.subcategory-image-container {
    position: relative;
    min-height: 180px;
    height: auto;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 10px;
}

.subcategory-image {
    max-width: 100%;
    max-height: 160px;
    width: auto;
    height: auto;
    object-fit: contain;
    transition: transform 0.3s ease;
}

.subcategory-placeholder {
    width: 100%;
    min-height: 160px;
    background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
    border-bottom: 1px solid rgba(0,0,0,0.1);
    display: flex;
    align-items: center;
    justify-content: center;
}

.subcategory-card:hover .subcategory-image {
    transform: scale(1.05);
}

.overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(25, 135, 84, 0.9);
    opacity: 0;
    transition: opacity 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
}

.subcategory-card.selected .overlay {
    opacity: 1;
}

.overlay-content i {
    color: white;
    font-size: 3rem;
    animation: checkmark 0.3s ease-in-out;
}

@keyframes checkmark {
    0% {
        transform: scale(0);
        opacity: 0;
    }
    50% {
        transform: scale(1.2);
    }
    100% {
        transform: scale(1);
        opacity: 1;
    }
}

.subcategory-card .card-title {
    font-size: 1rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
    transition: color 0.3s ease;
}

.subcategory-card.selected .card-title {
    color: #198754;
}

.subcategory-card .card-body {
    background: white;
    position: relative;
}

.subcategory-card.selected .card-body {
    background: rgba(25, 135, 84, 0.05);
}

/* Responsive adjustments */
@media (max-width: 768px) {
    .subcategory-image-container {
        min-height: 140px;
        padding: 8px;
    }
    
    .subcategory-image {
        max-height: 120px;
    }
    
    .subcategory-placeholder {
        min-height: 120px;
    }
}

@media (max-width: 576px) {
    .subcategory-image-container {
        min-height: 120px;
        padding: 6px;
    }
    
    .subcategory-image {
        max-height: 100px;
    }
    
    .subcategory-placeholder {
        min-height: 100px;
    }
    
    .overlay-content i {
        font-size: 2rem;
    }
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const subcategoryCards = document.querySelectorAll('.subcategory-card');
    const submitBtn = document.getElementById('submit-btn');
    const radioInputs = document.querySelectorAll('.subcategory-radio');
    
    subcategoryCards.forEach((card, index) => {
        card.addEventListener('click', function() {
            const subcategoryId = card.getAttribute('data-subcategory-id');
            
            // Remove selected class from all cards
            subcategoryCards.forEach(c => c.classList.remove('selected'));
            
            // Add selected class to clicked card
            card.classList.add('selected');
            
            // Check the corresponding radio button
            const radioInput = card.querySelector('.subcategory-radio');
            if (radioInput) {
                radioInput.checked = true;
            }
            
            // Enable submit button with animation
            if (submitBtn.disabled) {
                submitBtn.disabled = false;
                submitBtn.classList.remove('btn-secondary');
                submitBtn.classList.add('btn-success');
                submitBtn.style.animation = 'pulse 0.3s ease-in-out';
            }
            
            // Add a subtle feedback effect
            card.style.animation = 'selectPulse 0.3s ease-in-out';
            setTimeout(() => {
                card.style.animation = '';
                if (submitBtn.style.animation) {
                    submitBtn.style.animation = '';
                }
            }, 300);
        });
        
        // Add hover effects for better UX
        card.addEventListener('mouseenter', function() {
            if (!card.classList.contains('selected')) {
                card.style.borderColor = '#198754';
            }
        });
        
        card.addEventListener('mouseleave', function() {
            if (!card.classList.contains('selected')) {
                card.style.borderColor = '';
            }
        });
    });
    
    // Form validation with user feedback
    const form = document.querySelector('form');
    if (form) {
        form.addEventListener('submit', function(event) {
            const selectedRadio = document.querySelector('.subcategory-radio:checked');
            if (!selectedRadio) {
                event.preventDefault();
                
                // Visual feedback for missing selection
                subcategoryCards.forEach(card => {
                    card.style.animation = 'shake 0.5s ease-in-out';
                    card.style.borderColor = '#dc3545';
                });
                
                // Show alert with better styling
                const alertDiv = document.createElement('div');
                alertDiv.className = 'alert alert-warning alert-dismissible fade show mt-3';
                alertDiv.innerHTML = `
                    <i class="bi bi-exclamation-triangle me-2"></i>
                    Veuillez sélectionner une sous-catégorie avant de continuer.
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                `;
                
                form.insertAdjacentElement('afterend', alertDiv);
                
                // Reset card animations after shake
                setTimeout(() => {
                    subcategoryCards.forEach(card => {
                        card.style.animation = '';
                        card.style.borderColor = '';
                    });
                }, 500);
                
                // Auto-remove alert after 5 seconds
                setTimeout(() => {
                    if (alertDiv && alertDiv.parentNode) {
                        alertDiv.remove();
                    }
                }, 5000);
                
                return false;
            }
        });
    }
});

// Add CSS animations for better UX
const style = document.createElement('style');
style.textContent = `
    @keyframes selectPulse {
        0% { transform: translateY(-8px) scale(1); }
        50% { transform: translateY(-8px) scale(1.05); }
        100% { transform: translateY(-8px) scale(1); }
    }
    
    @keyframes shake {
        0%, 100% { transform: translateX(0); }
        25% { transform: translateX(-5px); }
        75% { transform: translateX(5px); }
    }
    
    @keyframes pulse {
        0% { transform: scale(1); }
        50% { transform: scale(1.05); }
        100% { transform: scale(1); }
    }
`;
document.head.appendChild(style);
</script>
{% endblock %}